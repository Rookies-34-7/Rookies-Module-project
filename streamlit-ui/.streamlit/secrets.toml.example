# 이 파일을 secrets.toml 로 복사해 값을 채우면 로컬에서도 secrets 경로로 동작합니다.
# Streamlit Community Cloud 는 앱 설정의 Secrets 입력란에 아래 내용을 그대로 붙여 넣습니다.
# secrets.toml 자체는 .gitignore 처리되어 있으니 절대 커밋하지 마세요.

OPENAI_API_KEY = "sk-여기에-키를-넣으세요"

# 선택. 지정하지 않으면 gpt-4.1-mini 를 씁니다.
# OPENAI_MODEL = "gpt-4.1-mini"
